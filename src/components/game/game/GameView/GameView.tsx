import * as React from 'react';
import './GameView.css'

interface IGameViewProps {

}

/**
*
* @returns {JSX.Element | null}
*/
export default function GameView(props: IGameViewProps): JSX.Element | null {
  return (
    <div>
      Game View
    </div>
  );
}