import * as React from 'react';
import './PawnSelector.css'

interface IPawnSelectorProps {
  
}

/**
*
* @returns {JSX.Element | null}
*/
export default function PawnSelector(props: IPawnSelectorProps): JSX.Element | null {
  return (
    <div className='pawn-selector-wrapper'>

    </div>
  );
}